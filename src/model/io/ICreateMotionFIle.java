package model.io;

/**
 * A interface that allows motion file creation.
 */
public interface ICreateMotionFIle {
  /**
   * A method that create the motion file.
   * @param name a String, name of the file
   */
  void createFile(String name);
}
